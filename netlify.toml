[build]
  publish = "."
  functions = "netlify/functions"
  command = ""

[functions]
  node_bundler = "esbuild"

[functions."analyze-sols"]
  timeout = 26

[functions."gmail-ingest"]
  timeout = 120
  schedule = "0 14 * * *"

[functions."send-rfq"]
  timeout = 30